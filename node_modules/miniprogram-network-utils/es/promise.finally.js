"use strict";
if (!Promise.prototype.finally) {
    Promise.prototype.finally = function (onfinally) {
        if (onfinally) {
            const P = this.constructor;
            // tslint:disable
            return this.then(value => P.resolve(onfinally()).then(() => value), reason => P.resolve(onfinally()).then(() => { throw reason; }));
        }
        else {
            return this;
        }
    };
}
//# sourceMappingURL=promise.finally.js.map