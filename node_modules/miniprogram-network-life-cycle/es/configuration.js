/**
 * 合并配置
 * @param customize 自定义配置，未定义的将被设置为默认值
 * @param defaults 默认值
 */
export function mergeConfig(customize, defaults) {
    const config = Object.assign(Object.assign({}, defaults), customize);
    if (defaults.headers && customize.headers) {
        // 合并headers
        config.headers = Object.assign(Object.assign({}, defaults.headers), customize.headers);
    }
    return config;
}
//# sourceMappingURL=configuration.js.map