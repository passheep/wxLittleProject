"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * 合并配置
 * @param customize 自定义配置，未定义的将被设置为默认值
 * @param defaults 默认值
 */
function mergeConfig(customize, defaults) {
    var config = __assign(__assign({}, defaults), customize);
    if (defaults.headers && customize.headers) {
        // 合并headers
        config.headers = __assign(__assign({}, defaults.headers), customize.headers);
    }
    return config;
}
exports.mergeConfig = mergeConfig;
//# sourceMappingURL=configuration.js.map